import cordova from 'movie-man/utils/cordova';
import { module, test } from 'qunit';

module('Unit | Utility | cordova');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cordova();
  assert.ok(result);
});
