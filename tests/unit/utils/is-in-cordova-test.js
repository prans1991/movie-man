import isInCordova from 'movie-man/utils/is-in-cordova';
import { module, test } from 'qunit';

module('Unit | Utility | is in cordova');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isInCordova();
  assert.ok(result);
});
