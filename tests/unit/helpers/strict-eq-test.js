import { strictEq } from 'movie-man/helpers/strict-eq';
import { module, test } from 'qunit';

module('Unit | Helper | strict eq');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = strictEq([42]);
  assert.ok(result);
});
